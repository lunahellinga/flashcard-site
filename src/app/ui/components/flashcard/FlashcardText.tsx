export const FlashcardText = ({text}:{text:string}) => {
    return (
        <div className="w-full h-full flex justify-center items-center text-black select-none text-center">{text}</div>
    );
};
